import React from 'react';
import {SafeView, Text} from '../../components';

const Pokedex = () => {
  return (
    <SafeView>
      <Text title="Pokedex" />
    </SafeView>
  );
};

export default Pokedex;
